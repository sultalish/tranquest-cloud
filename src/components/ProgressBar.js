import { useStatus, useEffect } from 'react';

const ProgressBar = () => {

  useEffect(() => {

  }, [])

  const divStyle = {
    width: progressWidth
  }

  return (
    <div style={divStyle}>

    </div>
  )
}
